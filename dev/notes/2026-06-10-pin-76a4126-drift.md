# Drift found at pin bump beec4325 → 76a4126 (M0)

`Rscript dev/book-gate.R` Symbols check flagged two tokens that do not resolve against the NAMESPACE at `76a4126294f6d8ddf762d2d073973b80fff3008b`:

- **`ptr_app_grid`** — documented by the retired `ggpaintr-use-cases.Rmd` (§ "Multiple plots and shared widgets — ptr_app_grid()") and still referenced in roxygen cross-references (`R/paintr-css-doc.R:3`, `R/paintr-server.R:38` and elsewhere), but **not exported**. The live `ggpaintr-tutorial.Rmd` § 3 covers multi-plot via `ptr_ui()` / `ptr_server()` + the `ptr_shared()` / `ptr_shared_panel()` / `ptr_shared_server()` trio instead. Book adaptation: `multi-plot.qmd` is written from tutorial § 3; the retired vignette's grid sections are excluded; plan M3/M6 amended accordingly.
- **`ptr_app_bslib`** — named only in the `theming-wrappers.qmd` stub scope line; no trace of an export at the pin. Book adaptation: chapter scope reworded to "writing your own theming wrapper" per the customization vignette's wrapper sections.

**Correction (2026-06-10, verified in the package repo):** an earlier version of this note claimed `ptr_app_grid` "does not exist" — false, the product of a truncated grep. Both functions are *defined* and *deliberately internal* at the pin: `ptr_app_grid` at `R/paintr-app.R:1360` and `ptr_app_bslib` at `R/paintr-app-bslib.R:55`, unexported by commit `d4d0656` ("docs(roxygen): … unexport app variants", `@export` → `@keywords internal`, Rd aliases retained so cross-links resolve). The operative fact for this book is unchanged — neither is public API at the pin, so the book documents neither and the gate rightly rejects the tokens — but there is **no package-side roxygen issue to file**: the cross-references resolve to real Rd targets by design.

## M4 additions — gallery sources vs the pin (verified by `formals()` + fixture boots)

`dev/scripts/gallery-examples.R` and the retired gallery vignette both drift from `76a4126`; the book's gallery chapter adapts as follows, with every adapted example proven by a booting fixture:

- Constructor arg `positional_arg =` does not exist — the pin takes `parse_positional_arg =` (used by `ppRange`, formerly also `colvars`). The `runtime =` arg used by the script's `ppVars2` does not exist either; `ppVars2` is dropped from the book.
- `ptr_arg_numeric_vector()` is not exported (and the script's `ptr_arg_string_vector` helper calls the internal `ggpaintr:::ptr_constant_fold`). The book's `ppRange` uses a public-API `arg_range()` parser (base `eval` in `baseenv()`) instead. Named without the `ptr_` prefix because the gate's Symbols check rightly owns that prefix.
- `colvars` (two positional args, old `positional_arg =`) dropped; `ppPercent` carries the § 6.5 custom-placeholder slot.
- `ppLayerOff(facet_wrap(...))` single-arg call in the script made explicit: `ppLayerOff(..., FALSE)`.
- Source bug fixed on copy: the script's second regression variant (`gallery-examples.R:149`) reused the mtcars title over iris data; book copy says "user-chosen model on iris".
- Retired-vignette originals modernized minimally: ggridges `..x..` → `after_stat(x)`; the bare `ggiraph` ggplot wrapped in `girafe(ggobj = )` so it renders as an htmlwidget.
- The retired vignette's ptr versions use pre-redesign keywords (`var`, `num`, `text`, `range`, `copy_defaults =`) — treated as historical, not copied.

## M6–M9 additions

- `ptr_shared()` at the pin has no `shared_ui =` argument (formals: `formulas, id, ui_text, expr_check, draw_all_label`); shared-key widgets auto-build. The retired use-cases vignette's `shared_ui =` examples adapted by dropping the argument (M6/M7 fixtures boot-verified).
- `ptr_app_bslib` is referenced by the retired customization vignette as a shipped demonstration wrapper but is **deliberately internal** at the pin (defined, unexported — see the correction above). `theming-wrappers.qmd` teaches the identical recipe as a user-written `my_app_bslib()`, boot-verified by the `theming-bslib` fixture.
- The **live** `ggpaintr-safety.Rmd` names `ptr_normalize_tabular_data()` as if callable; it is defined (`R/paintr-data.R:42`) but not exported (only `ptr_normalize_column_names` is). Book copy (`safety.qmd`) is reworded. Package-side fix **mooted by decision 2026-06-10**: the safety/llm content now lives in the book, and `vignettes/ggpaintr-safety.Rmd` is slated for deletion in the paired package PR, so the vignette will not be separately corrected.
